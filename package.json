{
  "name": "assistant-of-devil-backend",
  "version": "1.0.0",
  "description": "The real Devil's Assistant",
  "main": "server.js",
  "scripts": {
    "lint": "eslint src/**/*.ts tests/**/*/*.test.ts",
    "format": "eslint src/**/*.ts tests/**/*/*.test.ts --fix",
    "pretty": "prettier --write \"src/**/*.ts\" \"tests/**/*/*.test.ts\"",
    "build": "npx tsc && cp -r database/migrations dist/database/",
    "start": "node dist/src/server.js",
    "dev": "concurrently \"npx tsc --watch\" \"nodemon -q dist/src/server.js\"",
    "test": "jest",
    "migrate": "npm run build && sequelize-cli db:migrate",
    "migrate:undo": "npm run build && sequelize-cli db:migrate:undo",
    "migrate:undo:all": "npm run build && sequelize-cli db:migrate:undo:all",
    "migrate:status": "npm run build && sequelize-cli db:migrate:status",
    "migration:generate": "sequelize-cli migration:generate --name"
  },
  "author": "Volodymyr Bordiian",
  "license": "ISC",
  "dependencies": {
    "cli-color": "^2.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "http-status-codes": "^2.3.0",
    "openai": "^4.24.1",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.35.2"
  },
  "devDependencies": {
    "@types/cli-color": "^2.0.6",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.11",
    "concurrently": "^8.2.2",
    "eslint": "^8.56.0",
    "husky": "^8.0.3",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "prettier": "^3.1.1",
    "sequelize-cli": "^6.6.3",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
